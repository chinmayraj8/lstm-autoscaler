# Step 27: GitHub Actions CI -- lint + test, verified locally before landing
Date: 2026-09-04
Status: DONE. `.github/workflows/ci.yml` added; the parts of it verified
locally in this session are confirmed green; one part (`test-full`'s
TensorFlow install) wasn't locally re-run end-to-end for the reason
explained below, with the honest confidence level stated plainly rather
than assumed.

No CI existed anywhere in this project before this step -- every prior
"all tests pass" claim in this project's progress docs was a manual,
local `pytest` run. This step makes that automatic and gives every future
push/PR to `main` a real check.

## What changed

**`.github/workflows/ci.yml`** (new). Three separate jobs, not one:

- `lint` -- `ruff check src/ tests/`, using this repo's new
  `pyproject.toml` rule selection.
- `test-fast` -- installs everything in `requirements.txt` EXCEPT
  `tensorflow` (filtered out with `grep -v` at install time, not a
  separately maintained file, so it can't silently drift from
  `requirements.txt`), plus `requirements-dev.txt`, then runs
  `pytest tests/ --ignore=tests/test_api_shadow.py`.
- `test-full` -- installs the complete, unmodified `requirements.txt` +
  `requirements-dev.txt` (the exact pinned set the real Docker image
  uses -- Dockerfile parity, not a separate CI-only dependency set), then
  runs the complete suite including `test_api_shadow.py`.

All three trigger on push and PR to `main`. Python 3.11 in every job, to
match the Dockerfile's `python:3.11-slim` base exactly.

**Why split `test-fast`/`test-full` at all, rather than one job**:
`tests/conftest.py` has said, since it was written, that this suite "is
meant to run fast, in CI, without a TensorFlow install" (because
`src/autoscaler/__init__.py` only eagerly imports
config/calibration/data/decision/simulation/shadow/metrics_source/live_loop,
none of which need TensorFlow -- see that file's own docstring). That
was a documented design intent nothing had ever actually exercised until
this step. `test-fast` is what makes it a real, checked fact rather than
an assertion in a comment. Only `tests/test_api_shadow.py` needs
TensorFlow (it imports `src/api/main.py`, which imports `_build_lstm_model`
at module level), so it's the one file excluded from `test-fast` and the
reason `test-full` exists at all.

**`pyproject.toml`** (new). `[tool.ruff.lint] select = ["E4", "E7", "E9", "F"]`
-- pyflakes + pycodestyle's error classes only (unused imports/variables,
undefined names, syntax errors). Deliberately narrower than ruff's own
default rule selection: a bare `ruff check .` with no config pulls in a
much broader set (pyupgrade, flake8-comprehensions, flake8-simplify,
flake8-datetimez, flake8-bandit, and more -- 149 findings on this
codebase without this config file) that flags this project's own
intentional, documented patterns as bugs -- most importantly the
"log it, move on" broad `except Exception:` convention used throughout
`live_loop.py` and elsewhere, which flake8-bandit's `BLE001` would flag
on every occurrence. Locking the rule set in a committed config file
also means CI won't silently get stricter (or looser) on a future ruff
upgrade the way a bare `ruff check .` would.

**12 pre-existing lint findings fixed** (all genuinely dead code /
harmless, reviewed individually before fixing, nothing touching
actuation or decision logic):
- 9 unused imports (`src/api/demo_client.py`, `src/api/main.py`,
  `src/autoscaler/arima_baseline.py`, `src/dashboard/prepare_replay_data.py`,
  `tests/test_arima_baseline.py`, `tests/test_calibration.py`,
  `tests/test_live_loop.py`, `tests/test_shadow_store.py`)
- 1 f-string with no placeholders (`src/api/demo_client.py`)
- 1 genuinely dead local variable, `t0 = time.time()` in
  `src/dashboard/prepare_replay_data.py` -- assigned, never read;
  `wall_clock_secs` (returned by `_train_lstm` itself) was already doing
  the actual timing
- 1 import-order issue in `src/api/demo_client.py` (`import warnings`
  after a `sys.path.insert` statement) -- moved `import warnings` up
  to the top with the other stdlib imports, since it doesn't depend on
  the `sys.path` fix; `warnings.filterwarnings("ignore")` itself stayed
  where it was

## Local validation performed

- `ruff check src/ tests/` (with the new `pyproject.toml` config):
  0 findings, confirmed after the fixes above.
- `test-fast`'s exact job re-created locally in a clean venv: pinned
  `requirements.txt` versions minus `tensorflow`, plus
  `requirements-dev.txt`. Result: **117 passed, 0 failed** -- notably,
  this also resolved the one previously-known flaky test
  (`test_multistep_penalty_dilutes_a_single_step_spike...`,
  `3.9999999999999996 == 4.0`) that showed up repeatedly earlier this
  week under an unpinned/newer numpy. With the real pinned
  `numpy==1.26.4` from `requirements.txt`, that test passes cleanly --
  confirming it really was a numpy-version artifact, not a real bug in
  the decision engine, exactly as suspected when first seen.
- `.github/workflows/ci.yml` parsed and validated as well-formed YAML.
- **`test-full` was NOT locally re-run end-to-end in this session** --
  installing the real Linux `tensorflow==2.16.2` package timed out this
  session's local shell (TensorFlow is a large install; the shell here
  has a hard per-command time limit shorter than the install takes).
  Stated plainly rather than papered over: this one job's exact pass/fail
  outcome is inferred, not directly re-observed today. The inference is
  reasonably strong, not a guess -- this is the identical pinned
  `tensorflow==2.16.2` package (same `requirements.txt` line) already
  proven to install and run correctly under Linux in the real Docker
  image built and deployed to the cluster in Steps 23-25, and
  `test_api_shadow.py`'s own docstring documents exactly what it needs
  from TensorFlow (import-time model construction only -- no real
  dataset, no network, no `lstm_model.keras` file). First real GitHub
  Actions run of this workflow (next push) is the actual confirmation;
  noted below under "Still open" until that's observed.

## Before -> After

| | Before Step 27 | After Step 27 |
|---|---|---|
| CI | None -- every "tests pass" claim was a manual local run | `.github/workflows/ci.yml`, 3 jobs, runs on every push/PR to `main` |
| Lint | None | `ruff`, narrow rule set matched to this project's real conventions, 0 findings |
| "Tests run without TensorFlow" | An unverified claim in `conftest.py`'s docstring | An actual passing CI job (`test-fast`, 117/117) |
| The numpy floating-point flake | Reproducible, unresolved, explained away as "probably a version artifact" | Confirmed fixed by installing the real pinned `numpy==1.26.4` |

## Still open

- **`test-full`'s first real run is still pending** -- confirm on the
  next actual push to GitHub that it goes green (see "Local validation
  performed" above for why this is expected but not yet directly
  observed).
- No coverage reporting, no badge in `README.md` yet (`README.md` itself
  is still the original stale version -- out of scope for this step,
  deferred along with the rest of the README/LICENSE rewrite until after
  the full personal code read-through).
- No CI step builds/pushes the Docker image or deploys anything -- this
  step is lint + test only, on purpose; real-cluster deployment stays a
  deliberate, manual, watched action (see Step 26), not something CI
  does unattended.
- Every earlier step's own "still open" items are unchanged by this one.
